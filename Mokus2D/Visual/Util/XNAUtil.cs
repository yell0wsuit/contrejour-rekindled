using Microsoft.Xna.Framework;
using Microsoft.Xna.Framework.Graphics;

namespace Mokus2D.Visual.Util
{
    public static class XNAUtil
    {
        public static void LoadIdentityMatrix()
        {
            LoadWorldMatrix(Matrix.Identity);
        }

        public static void LoadWorldMatrix(Matrix matrix)
        {
        }

        public static void RefreshViewport(GraphicsDevice device)
        {
        }
    }
}
